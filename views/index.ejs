<p>
  hey
</p>

<p>
  Here's what your store is currently offering: 
</p>

  <% if (locals.food) { %> 
      <ul> 
      <% food.forEach(result=>{ %>
        <li><%= result.item%> <%= result.price%></li>
        <span> <%=result.ingredients%></span>
        
<!--    increase quant of item to order    -->
        <form id="add" onSubmit="add();">        
        <button value='<%=result.id%>'<type="submit"> + </button>
          
<!--    decrease quant of item to order    -->
        <form id="del" onSubmit="add();">        
        <button type="submit"> - </button>
          
      </ul>
      <% }); %>
  <% }%> 


<script type=text/javascript>
  function add() {
    var word = document.getElementById('delWord').value;
    console.log(word);
    fetch('https://aat38-assignment3parta.glitch.me/edit/'+word , {
        method: 'DELETE'
    }).then(resp=>{
      console.log("resp");
      console.log(resp);
    }, err=>{
      console.log(err);
    })
  }
</script> 